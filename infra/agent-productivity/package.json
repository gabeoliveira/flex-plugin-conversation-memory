{
  "name": "agent-productivity-infra",
  "version": "1.0.0",
  "private": true,
  "description": "Provisioning scripts for the agent-productivity analytics pipeline (dedicated Memory Store + Custom GenAI operator + Intelligence config + CO config). Sessions are CO-native (find-or-create by profile) — no Sync.",
  "type": "module",
  "scripts": {
    "create:store": "tsx create-agent-memory-store.ts",
    "create:operator": "tsx create-agent-operator.ts",
    "create:intel": "tsx create-agent-intelligence-config.ts",
    "create:co": "tsx create-agent-co-config.ts",
    "create:all": "npm run create:store && npm run create:operator && npm run create:intel && npm run create:co"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "@types/node": "^20.14.0"
  }
}
